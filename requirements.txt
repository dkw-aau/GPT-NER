openai==0.27.2
# simcse==0.4 must be installed separately with --no-deps to avoid its
# incompatible scipy/numpy constraints (see README for instructions).
faiss-cpu
numpy
scikit-learn
torch
tqdm
text2vec
python-json-logger
transformers
