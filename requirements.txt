# Core dependencies
pyyaml
simcse==0.4
faiss-cpu
numpy
scikit-learn
torch
tqdm
text2vec
python-json-logger

# LLM provider dependencies – install only the one(s) you need
#
# OpenAI (legacy <=0.28 or modern >=1.0 both supported):
#   pip install "openai>=0.27.2"
# Anthropic:
#   pip install anthropic
# HuggingFace Inference API:
#   pip install huggingface_hub

# Default: include the legacy OpenAI SDK so existing users are unaffected
openai>=0.27.2
